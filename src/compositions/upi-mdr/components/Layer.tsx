/**
 * Re-exported from the shared location so every composition — and the global
 * effects and overlay modules — use the same named layer, and no generic
 * "<AbsoluteFill>" row can appear in the Studio timeline.
 */
export {Layer} from '../../../layout/Layer';
